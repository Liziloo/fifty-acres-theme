<?php
$escape = $this->plugin('escapeHtml');
$this->headLink()->prependStylesheet($this->assetUrl('css/style.css'));

// Correct helpers for Theme-specific GUI assets
$logoUrl = $this->themeSettingAssetUrl('logo');
$bannerUrl = $this->themeSettingAssetUrl('banner');
$footerText = $this->themeSetting('footer_text');
?>
<?= $this->doctype(); ?>
<html lang="<?= $this->lang(); ?>">
<head>
    <?= $this->headMeta(); ?>
    <?= $this->headTitle(); ?>
    <?= $this->headLink(); ?>
    <?= $this->headScript(); ?>
</head>
<body>
    <?= $this->userBar(); ?>

    <div id="site-wrapper">
        <aside id="sidebar">
            <div class="branding">
                <a href="<?= $site->url(); ?>">
                    <?php if ($logoUrl): ?>
                        <img src="<?= $logoUrl; ?>" alt="<?= $escape($this->themeSetting('logo_alt')); ?>">
                    <?php else: ?>
                        <h1><?= $escape($site->title()); ?></h1>
                    <?php endif; ?>
                </a>
            </div>

            <nav id="main-nav">
                <?= $site->publicNav()->menu(); ?>
            </nav>

            <div id="sidebar-footer">
                <span class="est">EST. 1856</span>
                <?php echo $this->partial('common/search-form'); ?>
            </div>
        </aside>

        <main id="content-stage">
            <?php if ($bannerUrl): ?>
                <div id="global-banner"
                     role="img"
                     aria-label="<?= $escape($this->themeSetting('banner_alt')); ?>"
                     style="background-image: url('<?= $bannerUrl; ?>');">
                </div>
            <?php endif; ?>

            <div id="main-content">
                <?= $this->content; ?>
            </div>

            <footer id="site-footer">
                <?= $footerText ? $escape($footerText) : '© ' . date('Y') . ' Liz Casler Loomans'; ?>
            </footer>
        </main>
    </div>
</body>
</html>
